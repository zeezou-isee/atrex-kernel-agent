# CuTeDSL Pitfalls

| File | Kernel | Hardware | Trap count |
|------|--------|----------|-----------|
| [gdn-chunk-fwd-pitfalls.md](gdn-chunk-fwd-pitfalls.md) | `chunk_gated_delta_rule` forward (GDN chunk fwd, T=6144, bf16 q/k/v, fp32 state accum). V113 no-cache 3-kernel pipeline: precomputed Neumann + cp.async + LDSM/R2S + scaled-vnew state update + reuse-B LDSM + Bx2 non-tail launch. **#9-#13 (TMA)**: `elect_one` deadlock, D-1 all-warps-produce broken, `producer_commit` NOOP, TMA +55% regression, consumer thread mismatch. **#14-#21 (V31)**: direct global V RHS regression, K-decay scratch algebraic removal, transposed LDSM atom, output widening, FLA target discipline. **#22-#26 (V113 follow-up)**: no preprocess cache, split Bx2/B4 launchers, cache-policy/L2-window regression, NCU duration inflation, output-only TMA harness invalid | sm_120 (Pro5000 Blackwell) | 26 |
| [gdn-decode-pitfalls.md](gdn-decode-pitfalls.md) | `fused_recurrent_gated_delta_rule_fwd` (GDN decode, T=1, fp32 state, bf16 q/k/v) | sm_120 (Pro5000 Blackwell) | 12 |
| [nvfp4-gemm-pitfalls.md](nvfp4-gemm-pitfalls.md) | Fully-optimized persistent NVFP4 GEMM (BLOCK_K=128, STAGES=4, warp-specialized) | sm_120 (Pro5000 Blackwell) | 5 |
| [sm120-moe-data-prep-pitfalls.md](sm120-moe-data-prep-pitfalls.md) | INT32 MoE data-prep (histogram + per-CTA prefix offsets + scatter); V9 final, 3 single-rule attacks regressed (V8/V10/V11-A) + meta-rule | sm_120 (Pro5000 Blackwell) | 4 |
| [sm120-tma-warp-spec-pitfalls.md](sm120-tma-warp-spec-pitfalls.md) | TMA G2S + `PipelineTmaAsync` + 1-producer / 8-consumer warp-spec on cute 4.4.2 — 5 specific traps not covered by flashinfer NVFP4QuantizeTMAKernel (PEP 563 vs `@cute.struct`, struct as `@cute.kernel` arg, `MemRange[T,N]` factory, `ceil_div` MLIR legalization, Constexpr arg drop after `cute.compile`) | sm_120 (Pro5000 Blackwell) | 5 |
| [sm120-flash-attn-vllm-no-fast-path.md](sm120-flash-attn-vllm-no-fast-path.md) | `vllm.vllm_flash_attn.flash_attn_varlen_func` runs 16× slower than memcpy ceiling on sm_120 — same order as PyTorch SDPA; vllm dispatch likely falls through to slow generic CUDA reference (no per-arch fast path for sm_120) | sm_120 (Pro5000 Blackwell) | 1 |
| [sm120-ncu-l1-hit-rate-shared-pollution.md](sm120-ncu-l1-hit-rate-shared-pollution.md) | `l1tex__t_sector_hit_rate.pct` ("L1/TEX Hit Rate" in SoL/MWS) on sm_120 cute 4.4.2 + ncu 2025.2.1 includes ld.shared hits because L1 + texture share the same physical SRAM unit — TMA-based kernels appear to "not bypass L1" but actually do; verify via `sm__inst_executed_pipe_tma` vs `smsp__inst_executed_op_global_ld` ratio | sm_120 (Pro5000 Blackwell), ncu 2025.2.1 | 1 |
| [cute-442-vendor-flash-attn-pitfalls.md](cute-442-vendor-flash-attn-pitfalls.md) | Vendoring `flash_attn.cute` (Dao-AILab) on cluster cutlass-DSL 4.4.2: progressively-deeper `ImportError` chain, each requiring custom shim. cutlass 4.5+ introduced private-name convention (`_PipelineOp`, etc.) + re-export through `cutlass.utils.*` + restructured submodule paths — code targeting 4.5+ assumes all three; 4.4.2 has none. **Conclusion: not vendorable on 4.4.2 in finite patches; wait for cluster cutlass upgrade**. | sm_120, cute-DSL 4.4.2 vs flash_attn cute (4.5+) | 1 deep-trap (P1-P4 patch chain) |
| [sm103-paged-attention-decode-pitfalls.md](sm103-paged-attention-decode-pitfalls.md) | Paged-attention decode (hd256, GQA nqh=16, q_per_seq=4, block 64/256, bf16) v0→v10 tcgen05/split-KV journey: constexpr-unroll LLVM compile wall, dynamic `cutlass.range` TMEM-copy limit, latency-bound TMEM-occupancy wall, within-CTA overlap serial-O chain, TMA tensor-descriptor stall, tile-size regressions, Triton ceiling, hd256 FA4 prefill-kernel-not-usable-for-decode, + 5 silent-correctness traps | sm_103 (B300 Blackwell Ultra), CuTeDSL cutlass 4.5.2 | 14 |
| [sm103-fa4-hd256-prefill-pitfalls.md](sm103-fa4-hd256-prefill-pitfalls.md) | FA4 `flash_attn.cute` hd256 prefill: 2-CTA TMEM accumulator overflow → padding-NaN garbage, 1-CTA "cheap levers" null gains, ping-pong compile explosion, KV-stage pipeline-depth regression, vLLM ModelOpt NVFP4 version-skew | sm_103 (B300 Blackwell Ultra), CuTeDSL cutlass 4.5.2 | 5 |
| [sm100-vllm-fa4-hd256-seqused-pitfalls.md](sm100-vllm-fa4-hd256-seqused-pitfalls.md) | vLLM FA4 (CuTeDSL) hd256 2-CTA kernel hard-asserts `seqused_q/k` are None (TMEM capacity + `0*NaN` padding risk); `get_flash_attn_version()` silently downgrading FA4→FA2 | sm_100 (B200 / B300 Blackwell) | 2 |
## Cross-Architecture Pitfall Summary

- **Architecture Identification**: sm_120 ≠ sm_100; do not apply tcgen05 / wgmma / TMEM
- **cp.async Alignment**: `assumed_align=16` unlocks cp_size=128b; val_layout must match copy width (bf16 128-bit → (1,8) not (1,4)); SMEM row stride must be a multiple of 8 bf16
- **Cache Mode Diagnosis**: L1 hit > 50% on streaming workload = L2 false-saturation
- **Multi-Warp Topology Selection**: V-disjoint NOT K-distributed
- **MLIR API Boundary**: `.value` not `.ir_value`; `cpasync.CopyOp` abstract; `vector.extract` unstable
- **TMA Protocol**: Use `pipeline.PipelineTmaAsync` instead of manually writing mbarrier; **producer code must use warp-level guard (`if warp_id == 0:`) rather than thread-level (`if tidx == 0:`)** — `elect_one` inside cute.copy/producer_acquire is a warp-level primitive, a thread-level guard causes intra-warp divergence → deadlock
- **TMA D-1 Unavailable**: The D-1 mode of `PipelineTmaAsync` (all warps produce+consume simultaneously) deadlocks in actual testing — N warps each calling `arrive_and_expect_tx` causes arrival count underflow + N× tx_bytes mismatch; **warp specialization is mandatory**
- **TMA vs cp.async Choice**: TMA is **55% slower** than cp.async for small tiles (≤16KB) with serial dependencies (GDN chunk fwd measured 1.71ms vs 1.10ms); TMA only outperforms cp.async on large tiles (≥64KB) that can be pipelined
- **GDN V31/V113 Dataflow**: `sum_t (K[t,k] * exp_decay[t]) * v_new[t,v]` is equivalent to `sum_t K[t,k] * (exp_decay[t] * v_new[t,v])`; pushing decay to the v_new side removes K-decay scratch, and V113 further reuses `sS`/`sNK_A` B fragments, pushing the no-cache directional final_state path to 0.531-0.533ms
- **Direct Global V Is Not Inherently More Fused**: V30 bypasses shared memory and fetches V RHS directly from global — correct but slower (0.7200ms vs V29 0.6861ms); SASS reveals scalar `LDG.E.U16` falling on the RHS critical path; cp.async staging should still be retained on SM120
- **`cute.recast_tensor`**: Must be inside the dynamic K-loop; hoisting it outside the loop silently breaks multi-tile correctness (v38 NVFP4 GEMM)
- **CUTLASS-Style SF Pack Defaults to 8× Bloat**: `(128, 4)` per-atom layout only carries data in 1/8 of elements; using a compact layout trades out ~21 KB smem to fit BLOCK_K=128 + STAGES=4
- **cute-DSL 4.4.2 `cp.async.bulk` + `PipelineTmaAsync` mbar Incompatibility**: Following the CUTLASS-style "SF via TMA bar" path causes launch failure / garbled output / hang; all three workarounds (mbar wrapper / TMA-for-SF / cp.async.bulk + mbar_ptr) failed validation; cute-DSL 4.4.2's ceiling is 71% of CUTLASS
- **Three-Strike Signal for Multi-Bottleneck Kernels**: NCU single-rule % est. represents architectural ceiling, not wall-time gain. On INT32 MoE data preparation, V8 (warp-agg, 27.52% est.) / V10 (CUB sort, 48.85% est.) / V11-A (warp-specialization, 45.27% est.) — three single-rule attacks all regressed 24-41%; the only survivor V9-A is a 4 KB smem copy (cheap fix). See [cross-architecture meta-rules](../../../ref-docs/nvidia/common/ncu-rule-est-speedup-meta-rules.md) for details
- **`from __future__ import annotations` and `@cute.struct` Are Incompatible**: PEP 563 converts annotations to lazy strings, so cute cannot obtain the actual type object of `MemRange[T,N]` and throws `TypeError`. Any module containing `@cute.struct` must disable future-annotations.
- **`l1tex__t_sector_hit_rate.pct` Includes ld.shared**: On sm_120, L1 + texture share the same physical SRAM; TMA-bypass kernels may still show 30-50% L1 hit; use `sm__inst_executed_pipe_tma` vs `smsp__inst_executed_op_global_ld` to directly verify cache mode
- **vllm.vllm_flash_attn Has No Fast Path on sm_120**: 1645 us vs memcpy ceiling ~99 us = 16× slower; vllm dispatch falls onto the slow generic path on client Blackwell. Do not assume vllm approaches H100 performance on sm_120 when planning attention pipelines
- **Vendor flash_attn.cute Is Not Viable on cute 4.4.2**: cutlass 4.5 introduces `_PipelineOp` private-name + `cutlass.utils.*` re-export + submodule rearrangement; 4.4.2 has none of the three; shim speed cannot keep up with the gap. Wait for cluster cutlass to upgrade to 4.5+ before attempting cute-DSL FA fusion
